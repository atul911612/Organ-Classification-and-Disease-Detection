# Core deep-learning stack (install a CUDA build of torch for GPU training:
# see https://pytorch.org/get-started/locally/ for the right index-url).
torch>=2.2
torchvision>=0.17
timm>=1.0.7

# Data / metrics / plotting
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
pillow>=10.0
matplotlib>=3.7
pyyaml>=6.0
tqdm>=4.66
